'use client'
import { createTheme } from "@mui/material/styles"

export const defaultTheme = createTheme({
  palette: {
    mode: "light",
    primary: {
      main: "#494d6b",
      contrastText: "#FFFFFF",
    },
  },
})
